# Railway / Nixpacks build config.
# yt-dlp itself is bundled by the npm package `youtube-dl-exec` (downloaded on
# install), so only ffmpeg is needed as a system package for transcoding.
[phases.setup]
nixPkgs = ["nodejs", "ffmpeg"]

[start]
cmd = "node index.js"
